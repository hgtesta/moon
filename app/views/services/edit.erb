<%= breadcrumb :panels, :current_panel, :current_service, "Edit"  %>

<h2>Edit Service</h2>

<%= form_for [@panel, @service] do |f| %>
  <%= render "form", :f => f %>
  <%= f.submit "Save changes" %>
<% end %>

